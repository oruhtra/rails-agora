<div class="feed">
  <% @documents_unselected.each do |document| %>
    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-2">
      <%= link_to document_path(document) do %>
        <div class= "master-card">
          <div class="card-image" style="background-image: url('<%= cl_image_path document.photo, format: :jpg, density: 300 %>');">
          </div>
      <% end %>
          <div class="card-description">
            <div class="card-title">
              <% if document.name[45] %>
                <h5 class="truncate"><%= document.name[0..44] %>...</h5>
              <% else %>
                <h5><%= document.name %></h5>
              <% end %>
            </div>
            <div class="card-info">
              <p><%=document.updated_at.strftime("%Y-%m") %></p>
              <%= link_to document_path(document, document: {selected: true}), method: :patch do %>
              <i id="selectionbox" class="fa fa-square-o" aria-hidden="true"></i>
              <% end %>
            </div>
          </div>
        </div>
    </div>
  <% end %>
</div>
