<%= simple_form_for(@document) do |f| %>
  <%= hidden_field_tag(:user_id, @user.id) %>
  <%= f.input :name %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit %>
<% end %>
