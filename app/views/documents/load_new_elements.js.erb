if (typeof(resultsSection) === 'undefined') {
  var newDocSection = document.querySelector('.documents_new_wrapper');
  var reloadMasonryGrid = document.querySelector('.reload-masonry-grid');
}


newDocSection.innerHTML = "<%= j render 'results_section_documents_new'%>";
setTimeout(function(){reloadMasonryGrid.click();}, 800);
